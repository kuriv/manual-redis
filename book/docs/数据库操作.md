# 数据库操作

Redis 内部预置 16 个数据库（编号 0 ~ 15 ），默认选定 db0 。执行下面的命令，切换到指定数据库。

```
SELECT 1
```

执行下面的命令，查看当前所在数据库中键的总数量。

```
DBSIZE
```

执行下面的命令，异步清空当前所在数据库中的键。

```
FLUSHDB ASYNC
```

执行下面的命令，异步清空全部数据库中的键。

```
FLUSHALL ASYNC
```

执行下面的命令，将指定的键移动到其他数据库。

```
MOVE k1 1
```

