# 基数操作

执行下面的命令，将一个或多个元素添加到指定基数中，注意，存放的仅是算法摘要，而非原始数据。

```
PFADD login:20260101 Tom Jerry Spike
```

执行下面的命令，估算指定基数的去重总数。

```
PFCOUNT login:20260101
```

执行下面的命令，合并多个基数到新的指定基数。

```
PFMERGE login:total login:20260101 login:20250101
```
